# Python 3.11 — MMF1927H workshop
# Install:  python3.11 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
yfinance==1.5.2
pandas==3.0.5
pytrends==4.9.2
requests==2.32.5
python-dotenv==1.1.1
pyarrow==25.0.0

numpy==2.4.6

# Day 3 — feature engineering & modelling
scikit-learn==1.9.0
lightgbm==4.7.0        # needs the OpenMP runtime: `brew install libomp` on macOS
scipy==1.17.1
statsmodels==0.14.6

# Testing
pytest==9.1.1

# PDF export of slides / script / Q&A sheet (code/build_pdfs.py)
# macOS also needs the cairo+pango stack:  brew install pango
weasyprint==69.0
markdown==3.10.3
pypdf==6.4.0
